/// <reference types="react-scripts" />

type Status = {
  isActive: boolean
};

interface Goods {
  id: number,
  title: string,
  price: string,
  url: string,
  urlBig: string,
  isSelected: boolean
}
